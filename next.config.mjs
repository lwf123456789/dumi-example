/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized: true,
    }
    // images: {
    //     domains: ['cdn.iconscout.com','cdn-icons-png.flaticon.com'],
    // }
};

export default nextConfig;
